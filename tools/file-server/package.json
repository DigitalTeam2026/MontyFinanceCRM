{
  "name": "monty-file-server",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Local file storage server for Monty Finance CRM document storage. Writes uploaded files to the per-entity root folders configured in Admin Studio.",
  "main": "server.mjs",
  "scripts": {
    "start": "node server.mjs"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.57.4",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
